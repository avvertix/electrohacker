language: node_js
node_js:
  - 'node'

install:
  - travis_retry npm install

script: npm test

notifications:
  email:
    on_success: never


deploy:
  provider: releases
  api_key: "GITHUB OAUTH TOKEN"
  file: "FILE TO UPLOAD"
  skip_cleanup: true
  on:
    tags: true